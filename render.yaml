services:
  - type: web
    name: support-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt && npm install && npm run build
    startCommand: python backend.py